spring:
  datasource:
    url: jdbc:postgresql://db:5432/danuma-lk  # Use 'db' as the hostname
    username: postgres
    password: MyNewPass
    driver-classname: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql:true
      database: postgresql
      database-platform: org.hibernate.dialect.PostgreSQLDialect
